filters:
  and:
    - file.ext == "md"
formulas:
  seedlingDate: |-
    date("2026-05-03") -
    ((IF(whenToStartOutside>0, whenToStartOutside,whenToStartInside)*7).toString()+" days")
  Plant Date: date("2026-05-03")-((whenToStartOutside*7).toString()+" days")
properties:
  formula.seedlingDate:
    displayName: seedling_date
views:
  - type: table
    name: Table
    filters:
      and:
        - "!plantCategory.isEmpty()"
    order:
      - file.name
      - plantCategory
      - sili-seedlingCount
      - quantity
      - whenToStartInside
      - whenToStartOutside
      - formula.seedlingDate
      - formula.Plant Date
      - directSeed
      - plantSeason
      - growthDuration
    sort:
      - property: file.name
        direction: ASC
      - property: formula.seedlingDate
        direction: DESC
      - property: whenToStartInside
        direction: DESC
      - property: formula.Untitled
        direction: DESC
      - property: whenToStartOutside
        direction: ASC
    columnSize:
      note.growthDuration: 92
  - type: cards
    name: Seed Cards
    filters:
      and:
        - file.tags.contains("plant")
    order:
      - file.name
    image: note.image
